<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="view_partner_form_credit_authorization" model="ir.ui.view">
        <field name="name">res.partner.form.credit.authorization</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="account.view_partner_property_form"/>
        <field name="arch" type="xml">
            <field name="property_payment_term_id" position="after">
                <field name="can_authorize_credit" invisible="1"/>
                <field name="credit_authorized"
                       readonly="not can_authorize_credit"
                       widget="boolean_toggle"/>
            </field>
        </field>
    </record>

</odoo>
