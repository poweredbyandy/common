Adds Shift-based keyboard shortcuts to the Point of Sale.

While **Shift** is held (and the focus is not inside an input), letter
badges appear on top of the main POS buttons. Pressing that letter
triggers the button. Releasing Shift hides the badges.

This replaces function-key shortcuts (F1, F2, …) with a visual, discoverable
hint mode.
