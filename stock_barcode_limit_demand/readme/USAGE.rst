#. Open a transfer in the Barcode app.
#. Scan products until the done quantity matches the requested quantity.
#. Scan the same product again or keep adding units with the increment button.
#. The app shows an error and ignores the extra quantity.
#. Validate the transfer when all lines are complete.
