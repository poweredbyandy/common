This module prevents operators from scanning more units than the requested
quantity on a transfer in the Barcode app.

When the limit is enabled on the operation type, extra scans are rejected with
an error message and the quantity is not increased.
