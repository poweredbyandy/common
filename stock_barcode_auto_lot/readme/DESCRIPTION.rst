This module auto-assigns a lot or serial number when a tracked product is
scanned in the Barcode app. The generated number follows the same sequence as
stock lot generation and skips values already used on the transfer.

You can still open the line and change the lot or serial manually before
validation.
