Payments confirmed without outstanding accounts on the journal stay linked to
invoices through ``matched_payment_ids`` but leave no accounting entry. Once
the accounts are configured, users need a safe way to create those entries
without deleting and recreating payments.
