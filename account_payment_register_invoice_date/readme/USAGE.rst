Install the module and register a grouped payment for several customer or vendor
invoices as usual.

When the payment amount does not cover every selected invoice, the
reconciliation will apply funds starting with the invoice that has the earliest
``invoice_date`` (or accounting date when the invoice date is empty).
