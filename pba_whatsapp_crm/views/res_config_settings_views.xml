<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="res_config_settings_view_form_whatsapp_crm" model="ir.ui.view">
        <field name="name">res.config.settings.view.form.whatsapp.crm</field>
        <field name="model">res.config.settings</field>
        <field name="inherit_id" ref="crm.res_config_settings_view_form"/>
        <field name="arch" type="xml">
            <xpath expr="//app[@name='crm']" position="inside">
                <block title="WhatsApp CRM">
                    <setting
                        id="whatsapp_crm_auto_lead_setting"
                        string="Lead automático"
                        help="Crea una oportunidad al recibir un mensaje de WhatsApp. Los mensajes siguientes del mismo chat se asocian al lead durante 24 horas."
                    >
                        <field name="whatsapp_crm_auto_lead"/>
                    </setting>
                    <setting
                        id="whatsapp_crm_team_setting"
                        invisible="not whatsapp_crm_auto_lead"
                    >
                        <field name="whatsapp_crm_team_id"/>
                    </setting>
                    <setting
                        id="whatsapp_crm_assign_equally_setting"
                        invisible="not whatsapp_crm_auto_lead or not whatsapp_crm_team_id"
                        help="Distribuye los nuevos chats de WhatsApp entre los miembros del equipo seleccionado."
                    >
                        <field name="whatsapp_crm_assign_equally"/>
                    </setting>
                    <setting
                        id="whatsapp_crm_user_setting"
                        invisible="not whatsapp_crm_auto_lead or whatsapp_crm_assign_equally"
                    >
                        <field name="whatsapp_crm_user_id"/>
                    </setting>
                </block>
            </xpath>
        </field>
    </record>
</odoo>
