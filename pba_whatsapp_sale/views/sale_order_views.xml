<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_order_form_whatsapp" model="ir.ui.view">
        <field name="name">sale.order.form.whatsapp</field>
        <field name="model">sale.order</field>
        <field name="inherit_id" ref="sale.view_order_form"/>
        <field name="arch" type="xml">
            <xpath expr="//header" position="inside">
                <button
                    name="action_whatsapp_send_quotation"
                    type="object"
                    string="WhatsApp Presupuesto"
                    invisible="state not in ('draft', 'sent')"
                />
                <button
                    name="action_whatsapp_send_confirmed"
                    type="object"
                    string="WhatsApp Confirmación"
                    invisible="state != 'sale'"
                />
            </xpath>
        </field>
    </record>
</odoo>
