Bridge module that extends `pba_pos_ux` product search in the Point of Sale so
cashiers can find products by brand name, including wildcard patterns with `*`.

Loads product brands into the POS session and includes the brand in both the
local catalog filter and the server-side "search more" domain.
