1. Install `pba_pos_ux` and `product_brand`. This module auto-installs when both
   are present, or install it manually.
2. Open a Point of Sale session (reload/reopen so brand data is loaded).
3. In the product search, type a brand name or combine it with wildcards, e.g.
   `BOSCH` or `BOMB*BOSCH`.
