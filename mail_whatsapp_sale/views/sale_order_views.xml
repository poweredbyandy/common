<?xml version="1.0" encoding="utf-8" ?>
<odoo>
    <record id="sale_order_view_form_whatsapp" model="ir.ui.view">
        <field name="name">sale.order.form.whatsapp</field>
        <field name="model">sale.order</field>
        <field name="inherit_id" ref="sale.view_order_form"/>
        <field name="arch" type="xml">
            <xpath expr="//header" position="inside">
                <button
                    name="action_whatsapp_send_quotation"
                    type="object"
                    string="WhatsApp Cotización"
                    class="btn-secondary"
                    icon="fa-whatsapp"
                    groups="mail_whatsapp.group_mail_whatsapp_user"
                    invisible="state not in ('draft', 'sent')"
                />
                <button
                    name="action_whatsapp_send_order"
                    type="object"
                    string="WhatsApp Orden"
                    class="btn-secondary"
                    icon="fa-whatsapp"
                    groups="mail_whatsapp.group_mail_whatsapp_user"
                    invisible="state not in ('sale', 'done')"
                />
            </xpath>
        </field>
    </record>
</odoo>
