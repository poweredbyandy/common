1. Install `pba_pos_ux` and `pba_internal_code`. This module auto-installs when
   both are present, or install it manually.
2. Open a Point of Sale session (reload/reopen so internal codes are loaded).
3. In the product search, type an internal code or combine it with wildcards,
   e.g. `ABC123` or `ABC*123`.
