#. Go to **Settings > General Settings**.
#. Open the **Product Labels** block.
#. Enable **Show price on PDF labels** to print the price.
#. Disable it to print the same formats without the price.
#. The setting is per company.
